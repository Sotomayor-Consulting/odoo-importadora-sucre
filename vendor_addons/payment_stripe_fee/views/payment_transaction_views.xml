<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_transaction_form_stripe_fee" model="ir.ui.view">
        <field name="name">payment.transaction.form.stripe.fee</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount']" position="before">
    <field name="amount_service" string="Monto del servicio" invisible="provider_code != 'stripe'"/>
    <field name="stripe_fee" invisible="provider_code != 'stripe'"/>
</xpath>
<xpath expr="//field[@name='amount']" position="attributes">
    <attribute name="string">Total cobrado</attribute>
</xpath>
        </field>
    </record>
</odoo>