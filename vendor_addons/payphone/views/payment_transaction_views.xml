<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_transaction_form_payphone_fee" model="ir.ui.view">
        <field name="name">payment.transaction.form.payphone.fee</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount']" position="before">
                <field name="amount_service" string="Total servicio"
                       invisible="provider_code != 'payphone'"/>
                <field name="payphone_fee" string="Fee cobrado"
                       invisible="provider_code != 'payphone'"/>
            </xpath>
            <xpath expr="//field[@name='amount']" position="attributes">
                <attribute name="string">Total cobrado</attribute>
            </xpath>
        </field>
    </record>
</odoo>
