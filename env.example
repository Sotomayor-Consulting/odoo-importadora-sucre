# Copiar estos valores al panel de variables de entorno de Dokploy.
# Este archivo es solo una plantilla: NO poner aqui valores reales.

# --- PostgreSQL: superusuario (SOLO administracion y backups) ---
# Debe ser un nombre DISTINTO al rol de aplicacion de abajo.
POSTGRES_USER=odoo_admin
POSTGRES_PASSWORD=cambiar_por_una_clave_larga

# --- PostgreSQL: rol de aplicacion de Odoo (SIN superusuario) ---
# Lo crea db-init/10-odoo-role.sh en la primera inicializacion de la base.
# Con LOGIN y CREATEDB basta para que Odoo gestione sus propias bases.
ODOO_DB_USER=odoo
ODOO_DB_PASSWORD=cambiar_por_otra_clave_larga

# Nombre de la base de produccion. Debe coincidir con db_name/dbfilter de
# odoo.conf. El entrypoint la crea sola la primera vez.
DB_NAME=importadora_sucre

# Master password de Odoo (gestion de bases). La inyecta el entrypoint.
ADMIN_PASSWD=cambiar_por_una_tercera_clave_larga

# URL prefirmada del tarball de Odoo en Cloudflare R2.
# OJO: el NOMBRE de la variable va en MAYUSCULAS (R2_URL); docker-compose la
# referencia en 'secrets.r2_url.environment: R2_URL'. El nombre del secreto
# (r2_url, minusculas) es otra cosa y no debe cambiarse.
# Se genera con:
#   aws s3 presign s3://TU_BUCKET/odoo_19.0+e.20260902.tar.gz \
#     --endpoint-url https://TU_ACCOUNT_ID.r2.cloudflarestorage.com \
#     --expires-in 604800
# Caduca (maximo 7 dias). Ver docs/despliegue-dokploy.md, seccion "Caducidad".
R2_URL=https://TU_ACCOUNT_ID.r2.cloudflarestorage.com/...firma...

# Dominio publico (para el router del websocket en docker-compose.yml). Debe
# ser el MISMO que configuras como dominio del servicio en la UI de Dokploy.
DOMAIN=erp.tudominio.com

# Actualizacion de modulos en el arranque. Vacio en operacion normal.
# Se pone SOLO en el despliegue de actualizacion de Odoo y se vuelve a vaciar:
#   UPGRADE=all           -> actualiza todos los modulos (aplica migraciones)
#   UPGRADE=mi_modulo     -> actualiza solo ese modulo
UPGRADE=
